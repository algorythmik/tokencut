{
  "name": "tokencut-service",
  "version": "0.0.1",
  "type": "module",
  "description": "Local tokencut service — exact match cache backed by SQLite.",
  "scripts": {
    "start": "node --experimental-sqlite server.mjs"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "sqlite-vec": "^0.1.9"
  }
}
